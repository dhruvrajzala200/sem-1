#include<stdio.h>

int main(){
	float b,h,area;
	printf("enter value of base:");
	scanf("%f",&b);
	printf("enter value of height:");
	scanf("%f",&h);
	printf("area of triangle is %f",area=(b*h)/2);
}
